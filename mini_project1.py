# create a library class and define methods to display all thwe books, to lend books, to donate books, to return book
    # display book
    # lend book
    # add book
    # return book

# constructor to take in list of books and library name
# dictionary to see who owns the book if not present in the library.
# key = name of Book value = name of Person
# create a main function and run an infinite while loop asking users for their input


class Library:
    books = ["a","b","c","d","e"]

per1 = Library("Aks")
per1.lend